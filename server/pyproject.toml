[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.110", "uvicorn>=0.24.0", "sqlalchemy>=2.0", "psycopg[binary]>=3.1", "requests>=2.31.0", "pyyaml>=6.0", "httpx>=0.25.0" ]
requires-python = "~=3.12.0"
